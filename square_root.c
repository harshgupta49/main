#include<stdio.h>
#include<math.h>
int main()
{
    float x,y;
    printf("enter a number whose square root you want =");
    scanf("%f",&x);
    y=sqrt(x);
    printf("root = %f",y);

    return 0;
}

